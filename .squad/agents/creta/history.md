# Project Context

- **Owner:** Jorgito
- **Project:** GoogleBooksApp — ASP.NET Core Razor Pages app to search books via the Google Books API
- **Stack:** .NET 10, Razor Pages, IHttpClientFactory, Google Books API v1
- **Created:** 2026-08-16

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

- 2026-08-16: Toru scaffolded project structure: Configuration/GoogleBooksOptions, Models/GoogleBooks DTOs, Services/GoogleBooks typed-client seam, Pages/Books stub (TODOs remain). API key removed from appsettings.json and is now expected in dotnet user-secrets (UserSecretsId added to project).
- 2026-08-16: Creta built the `Pages\Books.cshtml` search experience with a POST search form, responsive results cards, thumbnail and broken-image fallbacks, empty/error messaging, and pagination links that preserve `TitleQuery` and `AuthorQuery`. The view reads Cinnamon's planned pagination/error properties (`PageIndex`, `TotalItems`, `HasNextPage`, `HasPreviousPage`, `ErrorMessage`) defensively via reflection so Razor still compiles until the code-behind lands, and assumes Google Books detail links can be built from `BookResult.Id` as `https://books.google.com/books?id={Id}`.
- 2026-08-16: Once Cinnamon confirmed the final `BooksModel` contract, the `Pages\Books.cshtml` top code block was simplified to direct property access for `ErrorMessage`, `PageIndex`, `TotalItems`, `HasNextPage`, and `HasPreviousPage` without changing the rendered UI.
- 2026-08-16: Creta unified the app styling around a light-blue and white palette with dark text (`#eef7ff`, `#d8ebff`, `#ffffff`, `#4a90e2`, `#2f6fb2`, `#111827`) and Inter/system typography by updating `Pages\Shared\_Layout.cshtml`, moving the Books page theme rules into `wwwroot\css\site.css`, and keeping the search, results, empty, error, pagination, and broken-cover states intact.
- 2026-08-17: Creta added an `About` nav item immediately after `Privacy` in `Pages\Shared\_Layout.cshtml` and implemented it as a Bootstrap modal (`#aboutModal`) so the app can show lightweight product/version details without creating a new Razor Page route; the modal styling reuses the shared light-blue palette variables and Inter typography in `wwwroot\css\site.css`.
- 2026-08-17T23:57:53Z: Full CI/CD promotion pipeline verified end-to-end; API keys configured for Dev/Qas/Prd App Services (GoogleBooks__ApiKey). - Scribe
- 2026-08-18: Creta implemented the advanced search filters UI for GitHub issue #5 by adding a collapsible `<details>` section to `Pages\Books.cshtml` with Language dropdown (ISO 639-1 codes), YearFrom/YearTo number inputs (1450-2100 range), and SortOrder dropdown (relevance/newest). Pagination links extended with `asp-route-language`, `asp-route-yearFrom`, `asp-route-yearTo`, and `asp-route-sortOrder` to persist filter state. Added CSS classes `.advanced-filters`, `.advanced-filters__toggle`, `.advanced-filters__content`, `.advanced-filters__select`, `.results-summary__note`, and `.results-summary__disclaimer` to `wwwroot\css\site.css` using existing design system custom properties. Included an asterisk and subtle disclaimer ("Results may be approximate when filtering by year") when year filters are active, per Cinnamon's backend TotalItems caveat. Native HTML `<details>` provides zero-JS progressive disclosure, keeping the primary title/author search uncluttered for casual users.
- 2026-08-18: Creta implemented the book details page UI for GitHub issue #6, replacing the placeholder `Pages\BookDetails.cshtml` with a full two-column responsive layout showing book cover (larger than search results), title, authors, full description, categories (as tag chips), page count, ISBN (prefers ISBN-13 over ISBN-10), "View on Google Books" button, and "← Back to search" navigation. Added `@using GoogleBooksApp.Models.GoogleBooks` directive for IndustryIdentifier type. Wired "View Details" links from `Pages\Books.cshtml` search results using `asp-page="/BookDetails" asp-route-id="@item.Id"` (page name refers to filename, not the `@page "/Books/Details/{Id}"` route override). Updated `.result-card__actions` in `wwwroot\css\site.css` to flex layout for side-by-side links. Added comprehensive book details CSS (`.book-details-page`, `.btn-back`, `.book-details__card`, `.book-details__layout`, `.book-details__cover`, `.book-details__tags`, `.btn-preview`) reusing all existing design system custom properties. Mobile-responsive with single-column layout <920px. Error state shows friendly "Book Not Found" message when `IsNotFound` is true, no technical details exposed. Build succeeded 0 errors, all 52 tests passed.
- 2026-08-19: Creta implemented loading spinner (GitHub issue #7) and dark mode toggle (GitHub issue #8) together in one pass. **Loading spinner:** Added CSS-only animated spinner overlay (`#search-spinner` in `Pages\Books.cshtml`) shown via JS event listeners on search form submit and pagination link clicks; uses existing design system color variables, respects `prefers-reduced-motion`, includes ARIA attributes for accessibility. **Dark mode toggle:** Added moon/sun emoji toggle button to `Pages\Shared\_Layout.cshtml` navbar; defined `[data-theme="dark"]` CSS custom properties override block in `wwwroot\css\site.css` (dark backgrounds #0f172a/#1e293b, light text #f1f5f9/#cbd5e1, adjusted primary blue #60a5fa/#93c5fd); localStorage persistence in `wwwroot\js\site.js` with inline `<head>` script to prevent flash-of-wrong-theme. All existing components (navbar, cards, modals, forms, pagination, book details, advanced filters) automatically adapt via custom properties. No external icon or image dependencies (Unicode emojis, pure CSS spinner). Build succeeded 0 errors, all 52 tests passed. Documented UI/UX decisions in `.squad\decisions\inbox\creta-spinner-darkmode.md`.

